---
name: recap
description: "prints a friendly prose recap of recent activity. use when the user says 'recap', 'what did we do', 'catch me up', 'summary of recent work', or any variant of requesting a recap."
allowed-tools: shell
---
<!-- generated by agent-scripts/gen_skills.py — do not edit; edit templates/recap.md.tmpl for shared wording or gen_skills.py's CAPABILITY_TABLE / *_PARAMS tables for harness-specific wording, then regenerate -->

Run:

```bash
DEVSTATUS_AGENT=1 python3 ~/.claude/scripts/dev_status.py recap
```

Display stdout verbatim — do not narrate, do not reformat.

If invoked with `--refresh` or if the user asks to refresh/regenerate the recap, pass `--refresh`:

```bash
DEVSTATUS_AGENT=1 python3 ~/.claude/scripts/dev_status.py recap --refresh
```
