# Runs the suite attended-by-default regardless of the ambient environment:
# an agent session carrying PI_AGENT_UNATTENDED=1 otherwise poisons the run
# (see test/helpers/sanitize-test-env.ts).
[test]
preload = ["./test/helpers/sanitize-test-env.ts"]
